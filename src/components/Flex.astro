---
type Props = {
  direction?: 'row' | 'column';
  gapRem?: number;
  justify?:
    | 'center'
    | 'flex-start'
    | 'flex-end'
    | 'space-between'
    | 'space-around'
    | 'space-evenly';
  align?:
    | 'center'
    | 'flex-start'
    | 'flex-end'
    | 'stretch'
    | 'baseline'
    | 'normal';
  paddingRem?: number;
  grow?: number;
};

const {
  gapRem = 0,
  direction = 'column',
  justify = 'flex-start',
  align = 'normal',
  paddingRem = 0,
  grow = 0,
}: Props = Astro.props;
---

<div
  style={{
    padding: `${paddingRem}rem`,
    display: 'flex',
    flexDirection: direction,
    gap: `${gapRem}rem`,
    justifyContent: justify,
    alignItems: align,
    flexGrow: grow,
  }}
>
  <slot />
</div>
