---
import Flex from './Flex.astro'

type Props = {
  title: string
  subtitle?: string
}

const { title, subtitle } = Astro.props
---

<Flex gapRem={1}>
  <h2>{title}</h2>

  {subtitle && <h3>{subtitle}</h3>}

  <Flex gapRem={0.25}>
    <slot />
  </Flex>
</Flex>
